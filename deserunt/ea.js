function generate(observer, invertible) {
    // Function implementation will go here
    console.log(observer);
    console.log(invertible);
}

// Calling the generate function with arguments
generate('exampleObserver', true);
